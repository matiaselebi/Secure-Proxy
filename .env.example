# Copiá este archivo a .env y completá tus valores reales.
# NUNCA subas el .env real a GitHub (ya está en .gitignore).

ABUSEIPDB_API_KEY=

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Token compartido con SecureHIPS para pedirle bloqueos. Tiene que ser el
# mismo string en el .env de los dos proyectos.
# Para generar uno: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECUREHIPS_API_TOKEN=
